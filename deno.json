{
  "lock": false,
  "tasks": {
    "dev": "deno run -A --watch=static/,routes/ dev.ts",
    "start": "deno run -A main.ts",
    "setup:hooks": "deno run -A -r https://deno.land/x/githooks/githooks.ts",
    "check": "deno fmt --check && deno lint",
    "pb:init": "deno run -A src/pb/init.ts",
    "pb:serve": "./pb/pocketbase serve",
    "pb:upgrade": "./pb/pocketbase update"
  },
  "githooks": {
    "pre-commit": "check"
  },
  "importMap": "./import_map.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "preact"
  },
  "lint": {
    "exclude": [
      "pb/",
      "output"
    ],
    "rules": {
      "tags": [
        "recommended"
      ]
    }
  },
  "fmt": {
    "exclude": [
      "pb/",
      "output"
    ],
    "useTabs": false,
    "lineWidth": 80,
    "indentWidth": 2,
    "proseWrap": "preserve"
  }
}
